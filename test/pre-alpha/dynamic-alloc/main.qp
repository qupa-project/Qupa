import "alloc.qp";
import "print.qp";

int main () {
	int size = sizeof#[i64]();
	size = size * 4;

	@char addr = malloc(size);
	@Array#[i64] arr = static_cast#[ @Array#[i64] ](addr);


	println(arr->[0]);
	println(arr->[1]);
	println(arr->[2]);

	free(addr);
	return 0;
}