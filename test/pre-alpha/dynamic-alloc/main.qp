import "../std.qp";

int main () {
	int size = SizeOf#[i64]();
	size = Mul(size, 4);

	@char addr = malloc(size);
	@Array#[i64] arr = static_cast#[@Array#[i64]](addr);


	println(arr->[0]);
	println(arr->[1]);
	println(arr->[2]);

	free(addr);
	return 0;
}